#include <stdio.h>
int main()
{
int AOT,b,h;
printf("the height of traingle is:");
scanf("%d" , &b);
printf("the base of traingle is:");
scanf("%d" , &h);
AOT=b*h*0.5;
printf("the area  traingle  is %d",AOT);

return 0;
}

